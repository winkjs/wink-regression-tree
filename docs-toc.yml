toc:
  - name: Creating an Instance
    description: |
      Load the **`wink-regression-tree`** module:

      `var regressionTree = require( 'wink-regression-tree' );`

  - regressionTree
  - name: API Methods
